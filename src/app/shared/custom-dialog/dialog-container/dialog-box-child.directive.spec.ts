import { DialogBoxChildDirective } from './dialog-box-child.directive';

describe('DialogBoxChildDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogBoxChildDirective();
    expect(directive).toBeTruthy();
  });
});
