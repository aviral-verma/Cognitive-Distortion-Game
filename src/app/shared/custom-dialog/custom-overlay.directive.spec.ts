import { CustomOverlayDirective } from './custom-overlay.directive';

describe('CustomOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
